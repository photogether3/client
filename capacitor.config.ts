import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.photogether.app',
  appName: 'photogether',
  webDir: 'dist/photogether/browser',
};

export default config;
